#include<stdio.h>
int my_strchr(char *str,char c){
    int *it
    while(*it!='\0'){
        if(*it==c){
            return it;
        }
        it=it+1;
    }
    return -1;
}
void find_all(char *str,char c,int pos,int *l){
    
    while(my(str,c)!="\0"){
        p
        l=l+1;
}
int main(){

    char * str="hello";
    char c="l";
    int pos[100];
    int *l
    find_all(str,c,pos,l);
}